# Foundation Reliability Evidence Ledger

Status values: `holds`, `narrowed`, `falsified`, `unverified`
Last updated: 2026-08-09

| Claim | Status | Current evidence | Next direct check |
|---|---|---|---|
| The canonical working-context projection is deterministic and hard-bounded. | holds | Existing tests plus the 2026-08-09 synthetic baseline: byte-for-byte deterministic, `1336` visible characters under a `6000` limit, cross-project and absolute-path canaries absent. | Preserve these guardrails in the first-slice acceptance test and repeat on MCP/iMac. |
| The default project working-context packet reliably carries a decision placed after a long project excerpt and shows its source. | falsified | Reproduced synthetic fixture: full README loaded, but packet admitted 4/5 decision identifiers; target recall `0`, provenance `0`, bytes `1341`, median `0.514ms`, p95 `1.164ms`; privacy and 6,000-character bounds passed. | Turn the characterization into a failing acceptance test after the Product Contract is confirmed. |
| Parsing only a standard project `## Decisions` section represents current user records. | falsified | Product docs say project READMEs contain decisions, but only 1 of 8 current top-level project records uses a `## Decisions`/`### Decisions` heading. | Decide between task-aware ordinary-prose retrieval and a real project-record schema with migration/maintenance behavior. |
| A new vector/graph ranker is required to retrieve the synthetic decision. | falsified | The existing lexical Markdown reader returns the target as its only result for the task query, with repo-relative source and line-addressable match. | Deepen the composition around the current reader first; add retrieval machinery only after a representative corpus defeats it. |
| `search_aios` scope `projects` is already a single-project privacy boundary. | falsified | MCP accepts `scope: projects` but no project identifier; `searchAios` scans the entire `projects/` tree. Session filters do not apply to Markdown projects. | Task-aware working context must restrict the selected project path before ranking and retain cross-project canaries in acceptance tests. |
| Current working-context selection is mainly project/recency-shaped; lexical search and intent do not rank injected context. | holds | `working-context.mjs:85-99,370-383` filters by project and recency and ranks sessions only by `captured_at`; the ranked lexical implementation in `search.mjs` is not used for startup selection. | Reproduce the user job on a seeded corpus and quantify decision recall before changing ranking. |
| The one search ranker currently governs the session digest. | falsified | `search.mjs:17-20` claims the session digest funnels through `searchAios`, but `working-context.mjs` never imports or calls search and ranks sessions only by timestamp. `digest.mjs` is a pass-through to working context. | Correct the misleading contract comment and make the deliberate boundary explicit before choosing whether task-aware evidence belongs in context or in expansion search. |
| Bridge pointer mode force-injects the full skill catalog every session. | falsified | 2026-08-07 excavation verification: pointer bridge files are about 1.2 KB and `skills_first` defaults false. | Preserve as a rejected framing. |
| Bridge instructions can cause a compliant agent to load the full skill index/resolver manually. | narrowed | Excavation verification: the catalog is not forced, but the managed instruction tells agents to read both generated files. | Measure current generated bytes and compare direct `skills resolve` output on realistic intents. |
| Stored research confidence metadata exists but retrieval ignores it. | holds | Excavation verification reports 78 research files with `confidence:` and no product read path; current code recheck pending. | Verify `search.mjs` frontmatter fields and fixture behavior before considering a read-only boost. |
| Multi-device private Git sync is safe for team or competing-writer claims. | falsified | 2026-08-01 and 2026-08-05 decisions restrict it to personal/private replication; AIOS sync receipt shows unresolved divergence failures. | Define writer authority, conflict UX, restore, and loss checks before any wider claim. |
| Git replication and MCP runtime access are the same memory authority. | falsified | Mission, handoff, repository guide, and accepted ADRs separate the canonical AIOS folder, read-only MCP, and optional Git replication. | Keep adapters separate in scenarios; do not create a shared seam until two real adapters justify it. |
| PR #59 is current, green, and mergeable at remote head `fa08b69`. | holds | `git ls-remote` and `gh pr view 59` on 2026-08-09: open, `CLEAN`, four successful Node 20/22 checks. | Keep untouched; reconcile only after the first-slice contract is settled. |
| `connect gemini` preserves user-authored `GEMINI.md`. | narrowed | The behavior is fixed and tested on PR #59 at `fa08b69`; it is absent from `origin/main` until reconciled. | Run the same regression independently in an isolated home and on the iMac comparison branch. |
| Windows project-skill junctions avoid absolute targets. | narrowed | Current code uses a relative target and junction; five Linux CI tests assert a non-absolute `readlink`. The independent iMac has no Windows runtime, so actual Windows junction behavior is not proven. | Add a real Windows CI/probe receipt before making a runtime support claim. |
| Hermes equivalent paths are deduplicated and the probe checks the production form. | falsified | Independent iMac trace: activation rewrites a legacy absolute path to `./skills` even when `./skills` already exists, leaving duplicates. Production attach resolves a relative path with `baseDir`; the probe writes/checks an absolute form with no `baseDir`. No Hermes invocation receipt exists. | Encode both failures as regressions, then fix the production adapter and bounded probe through one host contract. |
| Migration notices stay within the schema-change contract. | falsified | Independent iMac trace: migration notice logic exists only in CLI `brief.mjs`; MCP `read_working_context` returns the bare digest even though the tool and bridge contract claim the same canonical projection. | Specify whether notices belong inside or alongside the canonical projection; then make CLI/MCP behavior identical and test it. |
| The shipped onboarding contract is one consistent user journey. | falsified | `README.md` presents assistant-led installation; `docs/friend-setup.md` explicitly says not to ask a friend to paste the bootstrap into their own AI chat. | Choose one primary journey, designate the other as an explicit fallback, and add a doc-contract test. |
| The iMac launch work can replace PR #59 as a strict superset. | falsified | Live remote `fa08b69` and iMac `40e4170` diverge after common base `219d6cf`. Remote uniquely preserves user-authored `GEMINI.md` with four regression cases; the iMac six do not include that fix. | Reconcile commit-by-commit in an isolated branch; retain or independently reproduce the Gemini behavior and tests. |

## Evidence discipline

- A passing test proves only the behavior named by the test and the environment it ran in.
- A configured bridge, adapter, or skill is not supported until a fresh host discovers it and produces the expected observable result.
- An empty agent report is not clean evidence; every research or review receipt must expose failures and coverage.
- Derived metadata is rebuildable evidence, never the only durable copy of user knowledge.
- A stale local tracking ref is not remote topology evidence; compare the live remote before reasoning about ahead/behind state.

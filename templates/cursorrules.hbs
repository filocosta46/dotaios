Read `AGENTS.md` first.

Use `context/identity.md`, `context/priorities.md`, and `context/work.md` for user context.

For project work, read `projects/<slug>/README.md` and load any declared `context/domains/<domain>.md` files.

Keep memory local. Do not paste secrets. Ask before durable writes to `context/`, `vault/wiki/`, or `vault/org/`.

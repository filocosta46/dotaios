.env
.env.*
!.env.example
credentials.*
token.*
*.pem
*.key

memory/events.jsonl
memory/signals/
memory/errors.jsonl

.DS_Store
node_modules/

# Local project checkouts are repositories in their own right and never enter the AIOS mirror.
/workspaces/

# Regenerable OKF export bundles (dotaios export-okf) — never tracked or synced
build/

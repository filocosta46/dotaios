{
  "name": "dotaios-website",
  "private": true,
  "version": "1.0.0",
  "description": "DotAIOS marketing site (static + Sanity visual editing bundle)",
  "scripts": {
    "build:visual": "esbuild node_modules/@sanity/visual-editing/dist/index.js --bundle --format=esm --outfile=visual-editing.js --platform=browser"
  },
  "devDependencies": {
    "@sanity/client": "^7.6.0",
    "@sanity/visual-editing": "^5.4.3",
    "esbuild": "^0.25.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
